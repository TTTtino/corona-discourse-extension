if len(word_frequency['tokens']) > 0:
    parse_as_regex = word_frequency['parse-as-regex']
    measurement = word_frequency['measurement']
    frequency_r = frequency(tokens,parse_as_regex,measurement)
            `)